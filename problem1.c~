#include <stdio.h>
#include <stdlib.h>
int main(){
  
  int sum = 0;

  int x=0;
  for (x;x*5<1000;x++){
    sum+=x*5;
  }
  x=0;
  for (x;x*3<1000;x++){
    sum+=x*3;
  }
    
  
  printf("%d",sum);
  return 1;
}
